import { USER_LOGOUT, USER_LOGGIN } from "../constants"


export const register = () => async dispatch => {
    // asynchronous stuff here
}

export const login = () => async dispatch => {
    // asynchronous stuff here
}

export const logout = () => ({ type: USER_LOGOUT })